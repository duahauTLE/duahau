#include <stdio.h>

int main() {
    long long int n;
    scanf("%lld", &n);

    for (int i = 1; i <= n; i = i + 1) {
        for (int j = 1; j <= 2 * i - 1; j = j + 1) {
            printf("%d", j);
        }
        printf("\n");
    }

    return 0;
}
