#include <stdio.h>
#include <string.h>

int main() {
    char s[1000];
    fgets(s, sizeof(s), stdin);

    char s1[100];
    scanf("%s", s1);

    char *word;
    char *delim = " \n";

    word = strtok(s, delim);

    while (word != NULL) {
        if (strcmp(word, s1) == 0) {
            word = strtok(NULL, delim);
            continue;
        }
        printf("%s ", word);
        word = strtok(NULL, delim);
    }

    return 0;
}
