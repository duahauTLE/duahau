#include <stdio.h>
#include <stdbool.h>

bool gcd(long long a, long long b) {
    long long uoc = 1;
    for (int i = a; i >= 2; i--) {
        if (b % i == 0 && a % i == 0) {
            uoc = i;
        }
    }
    if (uoc == 1) {
        return true;
    } else {
        return false;
    }
}

int main() {
    long long a, b;
    scanf("%lld %lld", &a, &b);
    for (int i = a; i <= b; i++) {
        for (int j = i + 1; j <= b; j++) {
            if (gcd(i, j) == true) {
                printf("(%d,%d)\n", i, j);
            }
        }
    }
    return 0;
}
