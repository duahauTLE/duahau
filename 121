#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define MAX_WORDS 100


int isDuplicate(char *words[], int n, char *word) {
    for (int i = 0; i < n; i++) {
        if (strcmp(words[i], word) == 0) {
            return 1; 
        }
    }
    return 0; 
}

int main() {
    char s[100];
    fgets(s, sizeof(s), stdin);

    char *words[MAX_WORDS];
    int word_count = 0;

    char *token = strtok(s, " \n");
    while (token != NULL) {
        if (!isDuplicate(words, word_count, token)) {
            words[word_count++] = token;
        }
        token = strtok(NULL, " \n");
    }

   
    for (int i = 0; i < word_count; i++) {
        printf("%s ", words[i]);
    }

    return 0;
}
